{
  "name": "evolocity-pit",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^5.0.0",
    "express": "^4.17.1",
    "express-http-proxy": "^1.6.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-google-charts": "^3.0.15",
    "react-scripts": "3.1.1",
    "sqlite3": "^4.1.0"
  },
  "scripts": {
    "dev-react": "BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "dev-server": "nodemon server",
    "dev": "concurrently -n react,server npm:dev-*",
    "start": "NODE_ENV=production node server"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
